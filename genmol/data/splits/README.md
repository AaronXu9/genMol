# Frozen dataset split indices

Small JSON files defining train/val/test indices for each dataset. These ARE
committed to git so that experiments are reproducible.

Will be populated by `make prep-qm9`, `make prep-crossdocked`, `make prep-plinder`.

Currently empty.
