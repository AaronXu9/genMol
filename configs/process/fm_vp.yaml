# Variance-preserving cos/sin FM path (Albergo 2023).
# x_t = cos(π t / 2) x_0 + sin(π t / 2) x_1
# Use when linear path under-trains (recommended over LinearPath when the
# target distribution has the same variance as the base distribution).
_target_: genmol.process.flow_matching.FlowMatchingProcess
path: vp
